	ORG 100
IF, 	LOAD Y
	SUBT X
	SKIPCOND 800
	JUMP ENDIF

THEN, 	LOAD Y
	ADD 1
	STORE Y

ELSEIF, LOAD Z
	SUBT X
	SKIPCOND 800
	JUMP ENDIF
	LOAD Y
	SUBT 1
	STORE Y
  
ENDIF,	LOAD Z
	ADD ONE
	STORE Z
	HALT
X, DEC 3
Y, DEC 2
Z, DEC 0
ONE, DEC 1